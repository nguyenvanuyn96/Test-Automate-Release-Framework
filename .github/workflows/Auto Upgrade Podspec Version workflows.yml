name: "Auto Upgrade Podspec Version"

on: create

jobs:
  upgrade_podspec:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
        
    - name: Get and check if branch name is an `release/*`
      uses: uynguyen-wonderlabs/get-branch-name-github-action@master
      id: get-branch
      with:
          branch: ${{ github.ref }}
        
    - name: Get release version only
      if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
      id: get-release-version
      run: |
        version=$(echo ${{ github.ref }} | sed -e "s/refs\/heads\/release\///g")
        echo "::set-output name=new_release_version::${version}"
        echo ${version}

    - name: Create bot branch
      if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
      id: create_bot_branch
      run: |
          bot_branch="bot/${{ steps.get-branch.outputs.branch }}"
          echo "::set-output name=bot_branch::${bot_branch}"
          git checkout -b ${bot_branch}

    - name: Find and replace the current spec version to new version
      if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
      id: find_and_replace
      uses: shitiomatic/str-replace@master
      with:
        find: "\\t*s.version\\s*=\\s\"[\\S]*\""
        replace: "s.version      = \"${{ steps.get-release-version.outputs.new_release_version }}\""
        include: ".podspec"
    
    - name: Check result of Find and replace
      if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
      run: |
          echo "Number of files which have been modified = ${{ steps.find_and_replace.outputs.modifiedFiles}}"
           
    - name: Commit and Push changes
      uses: dciborow/commit@0.0.2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        push-branch: ${{ steps.create_bot_branch.outputs.bot_branch }}
        commit-message: "Upgrade podspec version to ${{ steps.get-release-version.outputs.new_release_version }}"
        force-add: 'true'
        force-push: 'true'
        # files: a.txt b.txt c.txt dirA/ dirB/ dirC/a.txt
        name: "Uy Nguyen"
        email: "uy.nguyen@wonderlabs.io"

    # - name: Commit the modified podspec file
    #   if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
    #   run: |
    #       git config --local user.email "uy.nguyen@wonderlabs.io"
    #       git config --local user.name "Uy Nguyen"
    #       git add -A
    #       git commit -m "Upgrade podspec version to ${{ steps.get-release-version.outputs.new_release_version }}"
    #       git status
          
    # - name: Push changes
    #   if: ${{ steps.get-branch.outputs.is_release_branch =='YES' }}
    #   uses: ad-m/github-push-action@master
    #   with:
    #       branch: ${{ steps.create_bot_branch.outputs.bot_branch }}
    #       github_token: ${{ secrets.NGUYENVANUYN96_WORKFLOW_SECRET }}
    #       force: true
    #       directory: .

    - name: Checkout
      uses: actions/checkout@v2
      with:
          ref: ${{ steps.create_bot_branch.outputs.bot_branch }}

    # - name: Invoke Auto PR workflow
    #   uses: benc-uk/workflow-dispatch@v1
    #   with:
    #     workflow: Auto PR
    #     token: ${{ secrets.NGUYENVANUYN96_WORKFLOW_SECRET }}
    #     inputs: '{ "release_branch": "${{ steps.get-branch.outputs.branch }}", "bot_release_branch": "${{ steps.create_bot_branch.outputs.bot_branch }}" }'
    
    # - name: Create artifact file
    #   run: |
    #     echo ${{ steps.get-branch.outputs.branch }} > release_branch.txt

    # - uses: actions/upload-artifact@master
    #   with:
    #       name: release_artifact
    #       path: release_branch.txt
